{
	"scripts": {
		"build": "esbuild ./src/main.js --bundle --sourcemap --format=esm --outdir=./build"
	},
	"dependencies": {
		"@mediapipe/tasks-vision": "^0.10.16",
		"esbuild": "^0.11.20",
		"three": "^0.127.0"
	}
}
